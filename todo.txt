Suggestions welcome!  Please open an Issue to recommend a feature or report a bug, or create a PR updating this file.

- use id-system parallel to naming (to handle same-name corner cases) e.g. cd [name|id]
- all commands: relative and absolute path support
- save terminal window position and size preference in storage, opacity too
- resizing from all four sides, + bottom right corner
- tree command?  would need to specify nof layers to ensure no request spam
- handle execution in other roots e.g. Shared w/ Me, Trash
- alias: local storage, define commands / arguments / locations

